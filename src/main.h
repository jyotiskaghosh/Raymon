#ifndef MAIN_H
#define MAIN_H

#define VIRTUAL_WIDTH 384
#define VIRTUAL_HEIGHT 216

#define WINDOW_WIDTH 1280
#define WINDOW_HEIGHT 720

#define TILE_SIZE 16

typedef enum {
	INTRO_MUSIC,
	FIELD_MUSIC,
	BATTLE_MUSIC,
	VICTORY_MUSIC,
	MAX_MUSIC
} MusicID;

typedef enum {
	BLIP_SOUND,
	POWERUP_SOUND,
	HIT_SOUND,
	RUN_SOUND,
	MAX_SOUNDS,
} SoundID;

typedef enum {
	TILES,
	PLAYER,
	CURSOR,

	AARDART_FRONT,
	AARDART_BACK,
	AGNITE_FRONT,
	AGNITE_BACK,
	ANOLEAF_FRONT,
	ANOLEAF_BACK,
	BAMBOON_FRONT,
	BAMBOON_BACK,
	CARDIWING_FRONT,
	CARDIWING_BACK,
	MAX_TEXTURES,
} TextureID;

typedef enum {
	BLINK,
	MAX_SHADERS
} ShaderID;

#endif